import { pt } from './pt'
import type { MainStrings } from './pt'

// Active locale — swap this import to change language (e.g. import { en } from './en')
const strings: MainStrings = pt

export { strings }
export type { MainStrings }
